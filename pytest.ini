[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: tests that download a model or take more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
